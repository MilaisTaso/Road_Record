<% if obj.errors.any? %>
<div class="row">
  <div class="col-8">
    <h5><%= obj.errors.count %>件のエラーが発生しました。</h5>
    <ul class="list-unstyled">
      <% obj.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>