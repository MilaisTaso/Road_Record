$("#relationships_btn").html("<%= j(render 'public/relationships/button', user: @user) %>");
if ($('#follower').length) {
  $('#follower').html("<%= j(render 'public/users/follower_card', user: @user) %>");
}